/** Re-export shared matrix engine — single source of truth with API. */
export {
  apartmentNumber,
  layoutFingerprint,
  matchScore,
  buildShaftMatrix,
  type ShaftMatrix,
} from '@lg/shared';
