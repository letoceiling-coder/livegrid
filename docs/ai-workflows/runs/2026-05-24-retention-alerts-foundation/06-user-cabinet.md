# 06 — User Cabinet

**Layout:** `AccountLayout.tsx` — tab nav + outlet

| Route | Page |
|-------|------|
| `/account/favorites` | Enhanced favorites grid |
| `/account/saved-searches` | Saved search management |
| `/account/history` | Browse history (100 cap) |
| `/account/notifications` | Notification center |

Legacy `/favorites` → redirect to `/account/favorites`.

Profile link preserved at bottom of cabinet.
