# 09 — Observability

## `?listing_debug=1`

Ecosystem block:
- published agencies/agents
- profile load count + timing
- discovery rank ms + cache hits/misses

## Admin metrics

`GET /admin/ecosystem/metrics`

DEV-only overlay; production requires auth.
